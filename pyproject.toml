[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "businessmanager"
version = "0.1.0"
description = "Executive business management simulation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
